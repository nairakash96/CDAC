module.exports={
    calcArea:function(rad){
        return Math.PI*rad*rad;
    },
    calcCircumfrence:function(rad){
        return 2*Math.PI*rad;
    },
    calcDiameter:function(rad){
        return 2*rad;
    }
}